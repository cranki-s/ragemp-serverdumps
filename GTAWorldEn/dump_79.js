{
/*var ALPRWindow = null;
mp.events.add('ALPR::toggle', (status) => {
	if (!mp.browsers.exists(ALPRWindow) && status)
	{
		ALPRWindow = mp.browsers.new("package://gtalife/ALPR/ALPR.html");
		alprStatus = true;
	}
	else if(ALPRWindow != null && mp.browsers.exists(ALPRWindow) && !status) 
	{
		ALPRWindow.destroy();
		ALPRWindow = null;
		alprStatus = false;
	}
});

mp.events.add('ALPR::update', (searching, name, plate, owner, insurance, license, stolen) => {
	if(ALPRWindow != null && mp.browsers.exists(ALPRWindow)){
		ALPRWindow.execute('InitializeALPR('+searching+', "'+name+'", "'+plate+'", "'+owner+'", '+insurance+', '+license+', '+stolen+');');
	}
});

*/
}